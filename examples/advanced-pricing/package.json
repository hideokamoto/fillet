{
  "name": "@pricectl/example-advanced-pricing",
  "version": "0.1.0",
  "private": true,
  "description": "Advanced pricing example with tiered and metered billing for pricectl",
  "scripts": {
    "build": "tsc",
    "synth": "pricectl synth",
    "deploy": "pricectl deploy",
    "diff": "pricectl diff",
    "destroy": "pricectl destroy"
  },
  "dependencies": {
    "@pricectl/cli": "workspace:*",
    "@pricectl/constructs": "workspace:*",
    "@pricectl/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
