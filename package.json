{
  "name": "@fillet/monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Infrastructure as Code for Stripe - AWS CDK-like tool for managing Stripe resources",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "prettier": "^3.1.0",
    "eslint": "^8.55.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hideokamoto/fillet.git"
  },
  "license": "MIT"
}
